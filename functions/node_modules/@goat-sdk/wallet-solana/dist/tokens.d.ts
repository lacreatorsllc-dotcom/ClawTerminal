import { Token as Token$1 } from '@goat-sdk/core';

type SolanaNetwork = "devnet" | "mainnet";
type Token = Token$1 & {
    mintAddress: string | null;
};
declare const SPL_TOKENS: Record<SolanaNetwork, Token[]>;

export { SPL_TOKENS, type SolanaNetwork, type Token };
