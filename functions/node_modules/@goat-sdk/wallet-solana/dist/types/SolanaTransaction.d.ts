import { TransactionInstruction, Keypair } from '@solana/web3.js';

type SolanaTransaction = SolanaInstructionTransaction;
type SolanaInstructionTransaction = {
    instructions: TransactionInstruction[];
    addressLookupTableAddresses?: string[];
    accountsToSign?: Keypair[];
    signer?: Keypair;
};

export type { SolanaInstructionTransaction, SolanaTransaction };
